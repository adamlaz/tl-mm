# Legacy AWS Manual Deployment
# Space: CCE
# Page ID: 257098114

Currently, cake is following the manual deployment process when equalling the prod stag and dev environments. This guide will describe the manual process of what we are proceeding with the AWS environments presently. as a current process, we are retrieving all ecs-related services via in build power shell script Once extracted data from the prod environment and dev environment we can do a manual comparison between the two spreadsheets. The important field we are carefully reviewing is I magePushedAt date if the imagePushedAt date is the latest in prod than dev environment we can choose that service as a required to update component. spreadsheet output sample New Method Now we can handle the deployments using the Jenkins Pipeline below. https://minuteman-build-jenkins.trycake.net/login?from=%2Fview%2FMinuteman%2520QA%2520Deployments%2Fjob%2FQA_deployPerComponent%2F TBC First, navigate to ECR and filter out the relevant repository by the filtering option. then filter by the image tag Get the relevant repositories URL from below view Then select cluster related to each service Filter-out relevant service that need to update you will navigate to below page. select task definition Normally we are not editing existing task definition for the new updates. instead to that we can create new task definition to run new service Select container and change image URl according to service version (The URL that copied from the ECR section) And update the service. Once created new task definition with new service URl you have to change existing task definition to the newly created task definition as below. (click update button and do the required changes) Once above process completed, we should monitor service state until it coming to the steady state.