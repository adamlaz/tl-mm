---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-900">{title}</h1>
  {description && <p class="mt-1 text-sm text-slate-500 max-w-2xl">{description}</p>}
</div>
